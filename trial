#include <iostream>
double dot(double r[3], double s[3], double y)
{
    y = r[0]*s[0] + r[1]*s[1] + r[2]*s[2];
}

int main()
{
    double a[3] = {1, 2, 3};
    double b[3] = {4, 5, 6};
    double ans = 0;
    dot(a, b, ans);
    std::cout<< ans;
}
